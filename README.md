# Base-Builders-
Building Base Agent 
unzip base-builder-agent.zip
cd base-builder-agent
# create an empty repo on github.com first, then:
git remote add origin https://github.com/YOUR_USERNAME/base-builder-agent.git
git branch -M main
git push -u origin main
agent call me 
repo activate 
